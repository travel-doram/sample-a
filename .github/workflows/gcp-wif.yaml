name: "Example Workload Identity"

on:
  push:
    branches:
      - "master"

jobs:
  run:
    name: "Workload Identity Job"
    permissions:
      id-token: write
      contents: read
    runs-on: "ubuntu-latest"
    steps:
      - name: "Auth in GCP"
        id: "auth"
        uses: "google-github-actions/auth@v1"
        with:
          token_format: "access_token"
          workload_identity_provider: projects/481994439242/locations/global/workloadIdentityPools/github-wif-ts-infra-demo/providers/github-wif-ts-infra-demo
          service_account: github-wif@ts-infra-demo.iam.gserviceaccount.com

      - name: "Docker login"
        run: |
          echo '${{ steps.auth.outputs.access_token }}' | docker login -u oauth2accesstoken --password-stdin https://europe-west4-docker.pkg.dev/ts-infra-demo/private
      - name: Check out the repository to the runner
        uses: actions/checkout@v4  
      - name: "build container"
        run: ENVPREFIX=demo ci/build.sh