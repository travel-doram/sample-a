on:
  push:
    branches:
      - master  


name: Dev CI/CD Workflow
jobs:
  some-job:
    name: run the job
    uses: ./.github/workflows/build_deploy.yaml
    permissions:
        id-token: write
        contents: read    
    with:
      environment: "Staging"